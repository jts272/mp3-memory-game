let game = {
  score: 0,
  currentGame: [],
  playerMoves: [],
  choices: [],
};

// Export with curly braces because we will be exporting more than one object
// and function.

module.exports = { game };
